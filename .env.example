# SQLite Configuration (Local Development)
SQLITE_DB_PATH=./data/churnguard_simulation.db

# BigQuery Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
# Local Development: Use file path to credentials
GOOGLE_APPLICATION_CREDENTIALS=./config/credentials/bigquery-credentials.json
# Production (Render): Use environment variable with full JSON content
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}

# PostgreSQL Configuration (Production)
DATABASE_URL=postgresql://username:password@host:5432/database_name

# Simulation Configuration
SIMULATION_START_DATE=2025-07-01
SIMULATION_END_DATE=2025-09-03

# Server Configuration
PORT=3000

# Debug mode
DEBUG=false